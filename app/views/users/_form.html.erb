<%= form_for(@user) do |f| %>
<% if @user.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

  <ul>
    <% @user.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="actions">
  <div>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div> 
  <div>
    <%= link_to t("general.back"), root_path, class: %w[button medium] %>
    <%= f.submit t('buttons.create'), :class => %w[button medium] %>
  </div>
</div>
<% end %>
